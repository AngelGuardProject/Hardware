{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Python: Current File (no debugging)",
        "type": "debugpy",
        "request": "launch",
        "program": "${file}",
        "console": "integratedTerminal",
        "env": {
          "VIRTUAL_ENV": "${workspaceFolder}/.venv"
        }
      }
    ]
  }